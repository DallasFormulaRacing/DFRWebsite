import { Config } from "../../../../types/ICPageConfig";

export default function ICLayout({ config }: { config: Config }) {
    return (
        <div>
            {/* Individual components here for each section */}
            Hello World!
        </div>
    )
}

